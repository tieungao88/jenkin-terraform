pipeline {
    agent any
    stages {
        stage("Terraform checking") {
            steps {
                echo "======Start======="
                sh 'terraform --version'
            }
        }
    }   
}